import { app, Component } from 'apprun';

export default class #nameComponent extends Component {
  state = '#name';

  view = state => <div>
    <h1>{state}</h1>
  </div>;

  update = {
    '##name': state => state,
  };
}

